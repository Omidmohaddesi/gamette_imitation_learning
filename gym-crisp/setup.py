from setuptools import setup

setup(name='gym_crisp',
      version='0.0.2',
      install_requires=['gym', 'numpy', 'matplotlib', 'flask', 'PyJWT', 'hashids']  # And any other dependencies required
      )
